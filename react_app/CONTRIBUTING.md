# Contributing

Small project. To run locally:

1. Install dependencies: `npm install`
2. Start dev server: `npm run start`

If you open a PR, include a short description of the change and a screenshot when relevant.
