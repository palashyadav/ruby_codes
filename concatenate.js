// Concatenate two strings

function concatenate(str1, str2) {
  return str1 + str2;
}

console.log(concatenate("Hello", " World"));    // Hello World
console.log(concatenate("Ruby", "JS"));          // RubyJS
console.log(concatenate("", "test"));            // test
